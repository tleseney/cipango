OPTIONS=sip
etc/cipango.xml
# jetty-deploy.xml should be call before cipango-sipapps.xml
etc/jetty-deploy.xml
etc/cipango-sipapps.xml

# To enable annotations, the following two lines should be uncommented
OPTIONS=plus,annotations
etc/cipango-annotations.xml

# TO enable the SIP web socket connector,  the following two lines should be uncommented
#OPTIONS=websocket
#etc/cipango-websocket.xml

# This configuration allows to see and filter SIP message send and received from cipango-console
etc/cipango-callflow.xml